name: devops-lab

services:
  web:
    build:
      context: .
    ports:
      - "127.0.0.1:8080:8080"
    init: true
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
