---
- name: Platform Engineering AWX Connection Test
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Verify AWX Execution
      ansible.builtin.debug:
        msg: "GitOps is live! GitHub Webhook automatically triggered this AWX job via ngrok."
